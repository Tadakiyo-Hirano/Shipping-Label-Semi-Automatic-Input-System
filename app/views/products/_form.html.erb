<%= form_with(model: @product, local: true) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :serial %>
  <%= f.text_field :serial %>

  <%= f.label :url %>
  <%= f.text_field :url %>

  <%= f.submit "#{yield(:button_text)}"  %>
<% end %>